# EO 构建时依赖声明（用户声明优先级最高，压过 import 自动检测）。
# fastapi 为 ASGI 框架检测与适配层依赖；mcp extra 内含 MCP SDK 本体。
fastapi
# 暂以 git 引用：#91 新增的 build_transport_security / configure_require_headers
# 与 requires-python >=3.10 降级尚未发布到 PyPI。必须钉 commit：main 上尚无这些
# 改动，脱钉拉 main 即构建失败，源分支删除后部署永久炸；发版后改为钉版本号
cnb-agentic-memory[mcp] @ git+https://cnb.cool/xqitw/cnb-agentic-memory.git@47dc1c8
